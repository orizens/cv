---
import Icon from "./Icon.astro";

interface Props {
  icon: string;
  title: string;
}
const { icon, title } = Astro.props;
---

<h2 class="cv-section-name">
  <Icon icon={icon} isLa />
  {title}
</h2>
