---
interface Props {
  icon: string;
  title: string;
}
const { icon, title } = Astro.props;
---
<h2 class="cv-section-name">
  <span class={`fa fa-${icon} section-title-icon`}></span> {title}
</h2>
